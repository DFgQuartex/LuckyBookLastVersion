<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/fragment_picture_selector"/>
    <RelativeLayout
        android:id="@+id/footer"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:layout_alignParentBottom="true"
        android:background="#ccffffff"
        android:clickable="true">

        <Button
            android:id="@+id/category_btn"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_centerVertical="true"
            android:background="@null"
            android:drawableEnd="@drawable/ic_expand_more_black_24dp"
            android:drawablePadding="@dimen/half_view_margin"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:paddingLeft="@dimen/twice_view_margin"
            android:paddingRight="@dimen/twice_view_margin"
            android:singleLine="true"
            android:textAllCaps="false"
            android:textColor="@color/mis_folder_text_color"
            android:textSize="16sp" />

    </RelativeLayout>

</RelativeLayout>